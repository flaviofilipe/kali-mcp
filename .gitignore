# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Segredos (Cognito, tokens etc.)
.env
.env.*

# Config local do Claude Code (paths de máquina, allowlists de permissão —
# nunca deve ir pro histórico do git, independente de config global)
.claude/settings.local.json
